# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## v0.2.0 - 2026-07-30

- 新增全局非安全内容 LLM 与独立预设人设，非安全任务默认直接生成，只有勾选对应圆桌触发时机时才进入非安全圆桌。
- 常规与非安全故事生成链路分别使用配置的模型提供商、人设、结构修复和圆桌回退。
- 为每名玩家与 NPC 持久化 0 至 100 的生命值和淫乱值，兼容迁移既有房间及存档数据。
- 全局判断 LLM 根据已生成的客观结果裁定扣血；生命值跨过 20 点档位时每轮生成至多一张战损 CG，归零后标记死亡和尸体。
- 40 及以下生命值使用严重战损提示，固定杀害选项无论普通伤害裁定如何都将目标生命值归零。
- 淫乱值按百分比影响首次相遇时的角色自主事件概率，每轮最多触发一次，并由全局非安全内容链路生成后作为独立消息发送和缓存。
- 移除插件侧年龄及 18+ 内容过滤，插件仅负责内容模型路由，最终可用性由上游模型提供商决定。
- `/查看故事` 支持按原分段重发行动结果和随机事件；人物改图继续固定使用首次 CG 作为一致性基底。
- 回归测试扩充至 91 项，覆盖双模型路由、数值迁移、扣血里程碑、死亡状态、CG 去重、概率边界和分段缓存。

## v0.1.3 - 2026-07-30

- 修复 OneBot 群聊中 `@机器人 3` 等简短选项被全局判断模型误判为普通聊天，继而落回 AstrBot 普通回复的问题。
- 明确的 `1/2/3`、`选3`、`第三项`等表达改为验证当前候选项后确定性路由，不再依赖轻量模型猜测意图。
- 消息监听与 `interactive_fiction` 函数工具统一采用相同的选项解析逻辑；无效编号及普通含数字聊天不会被误接管。
- 收紧不合理行动拒绝边界：仅拒绝主角依据当前状态无法实施的行动，以及玩家自行宣告尚未裁定结果的内容；非安全题材不再构成拒绝原因。
- 回归测试扩充至 77 项，覆盖 OneBot `At + Plain` 消息链、常见选项表达、双入口路由及非安全行动合理性。

## v0.1.2 - 2026-07-29

- 新增全局故事生成 LLM 及可编辑预设人设，默认负责底稿、角色创建和故事行动。
- 新增六类圆桌会议触发时机，默认只勾选非安全内容与底稿；未命中时直接采用全局故事输出。
- 行动时并发调用全局判断与全局故事生成模型；单独提案模式把故事输出交给评审，轮流讨论模式从首个提案模型继续。
- 全局故事模型未配置或失败时兼容回退原圆桌链路；普通直接生成不覆盖最近圆桌会议记录。
- 按 AstrBot 官方 `template_list.display_item` 规范修复圆桌模型折叠副标题，组合显示名称、身份和内容类型。
- 将回复重发指令从 `/故事 继续` 改为 `/查看故事`，`/故事 继续` 现在作为玩家行动处理。
- 开局首次人物立绘把当前可见环境同时提供给提示词模型，并拼接进最终生图提示词。
- 回归测试扩充至 71 项，覆盖直接生成、圆桌触发、底稿传递、并发路由、新指令和环境提示词。

## v0.1.1 - 2026-07-29

- 修复开局和新玩家加入产生的圆桌讨论未保存，导致 `/圆桌会议` 无法查看最近生成记录的问题。
- 修复多人回档移除角色后，自然语言行动不会提示重新加入、也无法自然语言重入原房间的问题。
- 提案模型全部失败时立即终止生成，不再错误地让评审脱离提案直接输出内容。
- 指令行动在调用全局判断模型前检查房间行动锁，避免已锁定时重复消耗模型请求。
- 待选故事状态支持直接使用 `/故事 [编号/名称/全随机]` 选择，关闭自然语言时仍可完整操作。
- 后台绘图增加回档身份校验和发送异常取消处理；未启用绘图条目时跳过图像判断模型。
- 加强存档回溯、损坏状态文件、结构化选项、ComfyUI 节点映射及图片下载大小的防御性校验。
- 移除 AstrBot 已弃用的 `@register` 装饰器，使用框架自动发现与 `metadata.yaml` 元数据。
- 回归测试由 52 项扩充至 62 项，并继续覆盖真实 AstrBot 指令与函数工具注册。

## v0.1.0 - 2026-07-29

- 圆桌模型折叠副标题改为“名称——身份——生成的内容类型”，并自动维护中文显示值。
- 自然语言判断仅在玩家已有故事或处于待选故事状态时启用，未开局聊天不再调用全局判断模型。
- 新增 `/故事 继续`、`/故事 1/2/3` 和 `/故事 [自由行动]`，最近行动的文字、选项及已生成图片可重复发送。
- 继续故事固定显示三项圆桌选项；交谈态追加杀害、侵犯和自由行动，非交谈态追加自由行动。
- 开局顺序调整为先发送环境，再发送人物介绍与四项开局选择，首次立绘改为后台生成。
- 新增人物首次登场、首次对话、杀害、侵犯、场景变换和人物战损六类图像生成时机配置。
- 图像时机判断、生成和发送改为后台任务，不阻塞故事正文发送或房间后续行动。
- 加强圆桌结构化输出校验、JSON 容错、流式片段兼容及一次自动格式修复。

## v0.0.4 - 2026-07-29

- 开局按个人信息、视野所及环境、人物立绘的顺序分段发送，环境内容不泄露不可感知的隐藏设定。
- 新增可展开的全局字数限制配置，分别控制常规正文、非安全正文、个人信息、环境和心理描述提示词。
- OpenAI 兼容与 ComfyUI 绘图条目新增画风配置，并将规范化画风前缀写入最终生图或改图提示词。
- 兼容迁移旧版故事条目中未修改的默认 `30/200` 字内容限制，避免与新的全局数字配置冲突。

## v0.0.3 - 2026-07-29

- 修复故事、圆桌模型、OpenAI 兼容、ComfyUI 工作流和自定义节点折叠条目不显示名称副标题的问题。
- 更新人物绘图条目说明，明确首次文生图、后续缓存图改图及优先级失败切换机制。
- 将圆桌模型“生成的内容类型”移动到模型提供商下方。
- 将单个人设输入框拆为条件显示的常规内容与非安全内容人设，各自提供独立默认预设。
- 保留并迁移 `v0.0.2` 已自定义的旧版圆桌人设。
- 使用新的 `/故事` 帮助菜单，并扩充面板、迁移与帮助文本回归测试。

## v0.0.2 - 2026-07-29

- 将圆桌模型条目改为默认启用，并为圆桌模型与全局判断模型提供可编辑的预设人设。
- 为非安全圆桌模型提供按身份动态补全的高强度暴力、血腥和性题材创作人设。
- 故事、圆桌模型、OpenAI 兼容、ComfyUI 工作流与自定义节点条目统一以名称作为折叠副标题。
- 注册 AstrBot 原生故事、存档、读档和圆桌会议指令，以及 `interactive_fiction` LLM 函数工具。
- 修复并发行动被静默忽略和自然语言越界槽位可能转为自动存档的问题。
- 扩充配置、圆桌、存档、回溯、记忆、绘图路由、行动锁和 AstrBot 注册回归测试。

## v0.0.1 - 2026-07-29

- 首次公开版本。
- 支持自然语言与指令两种操作方式，并保留 AstrBot 正常聊天链路。
- 支持单人故事和 OneBot 群聊多人同游。
- 支持多模型圆桌提案、评审、优先级、讨论轮数和失败回退。
- 支持故事条目、玩家行动限制、内容限制与动态剧情节奏。
- 支持四个个人手动存档槽、自动存档和多人世界回溯。
- 支持全部记忆、长期自动压缩和临时上下文压缩。
- 支持 OpenAI 兼容图像接口与本地 ComfyUI 工作流。
- 支持首次登场立绘、后续改图以及 OneBot 合并转发圆桌记录。
